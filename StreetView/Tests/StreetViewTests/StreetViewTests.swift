import XCTest
import ComposableArchitecture
import Quick
@testable import StreetView

final class StreetViewTests: QuickSpec {
    override func spec() {
        it("Test when the screen appears we need to fetch a random point") {

        }
    }
}

/*
 
 struct Point {
     let lat: Double = 0.0
     let lng: Double = 0.0
 }
 
 1 - Buscar um ponto aleátorio lat/lng: quando a tela aparecer, precisamos buscar um ponto aleatório
 
 */
